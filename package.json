{
  "name": "my-theme-park",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "setup-dbs": "psql -f ./db/setup.sql",
    "seed:dev": "NODE_ENV=development node ./db/seeds/run-seed.js",
    "seed:test": "NODE_ENV=test node ./db/seeds/run-seed.js",
    "seed": "NODE_ENV=development node ./db/seeds/run-seed.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "pg": "^8.16.0",
    "pg-format": "^1.0.4"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.1"
  }
}
